{
  "name": "tushare_libor",
  "description": "Libor利率。Libor拆借利率限量：单次最大4000行数据，总量不限制，可通过设置开始和结束日期分段获取积分：用户积累120积分可以调取，具体请参阅积分获取办法。返回字段包括：date(Y), curr_type(Y), on(Y), 1w(Y), 1m(Y), 2m(Y), 3m(Y), 6m(Y), 12m(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "日期 (日期输入格式：YYYYMMDD，下同)",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      },
      "curr_type": {
        "type": "string",
        "description": "货币代码  (USD美元  EUR欧元  JPY日元  GBP英镑  CHF瑞郎，默认是USD)"
      }
    },
    "required": []
  }
}