{
  "name": "tushare_income",
  "description": "利润表。获取上市公司财务利润表数据积分：用户需要至少2000积分才可以调取，具体请参阅积分获取办法提示：当前接口只能按单只股票获取其历史数据，如果需要获取某一季度全部上市公司数据，请使用income_vip接口（参数一致），需积攒5000积分。。返回字段包括：ts_code(Y), ann_date(Y), f_ann_date(Y), end_date(Y), report_type(Y), comp_type(Y), end_type(Y), basic_eps(Y), diluted_eps(Y), total_revenue(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "ann_date": {
        "type": "string",
        "description": "公告日期（YYYYMMDD格式，下同）",
        "pattern": "^\\d{8}$"
      },
      "f_ann_date": {
        "type": "string",
        "description": "实际公告日期",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "公告开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "公告结束日期",
        "pattern": "^\\d{8}$"
      },
      "period": {
        "type": "string",
        "description": "报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报)"
      },
      "report_type": {
        "type": "string",
        "description": "报告类型，参考文档最下方说明"
      },
      "comp_type": {
        "type": "string",
        "description": "公司类型（1一般工商业2银行3保险4证券）"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}