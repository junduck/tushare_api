{
  "name": "tushare_opt_mins",
  "description": "期权分钟行情。获取全市场期权合约分钟数据，支持1min/5min/15min/30min/60min行情，提供Python SDK和 http Restful API两种方式。限量：单次最大8000行数据，可以通过合约代码和时间循环获取。权限：120积分可以调取2次接口查看数据，正式权限请参阅 权限说明  。。返回字段包括：ts_code(Y), trade_time(Y), open(Y), close(Y), high(Y), low(Y), vol(Y), amount(Y), oi(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码，e.g：10007976.SH"
      },
      "freq": {
        "type": "string",
        "description": "分钟频度（1min/5min/15min/30min/60min）"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期 格式：2024-08-25 09:00:00",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束时间 格式：2024-08-25 19:00:00",
        "pattern": "^\\d{8}$"
      }
    },
    "required": [
      "ts_code",
      "freq"
    ]
  }
}