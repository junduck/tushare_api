{
  "name": "tushare_fund_manager",
  "description": "基金经理。获取公募基金经理数据，包括基金经理简历等数据限量：单次最大5000，支持分页提取数据积分：用户有500积分可获取数据，2000积分以上可以提高访问频次。返回字段包括：ts_code(Y), ann_date(Y), name(Y), gender(Y), birth_year(Y), edu(Y), nationality(Y), begin_date(Y), end_date(Y), resume(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "基金代码，支持多只基金，逗号分隔"
      },
      "ann_date": {
        "type": "string",
        "description": "公告日期，格式：YYYYMMDD",
        "pattern": "^\\d{8}$"
      },
      "name": {
        "type": "string",
        "description": "基金经理姓名"
      },
      "offset": {
        "type": "string",
        "description": "开始行数"
      },
      "limit": {
        "type": "number",
        "description": "每页行数"
      }
    },
    "required": []
  }
}