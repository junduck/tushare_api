{
  "name": "tushare_fund_adj",
  "description": "ETF复权因子。获取基金复权因子，用于计算基金复权行情限量：单次最大提取2000行记录，可循环提取，数据总量不限制积分：用户积600积分可调取，超过5000积分以上频次相对较高。具体请参阅积分获取办法。返回字段包括：ts_code(Y), trade_date(Y), adj_factor(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "TS基金代码（支持多只基金输入）"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期（格式：yyyymmdd，下同）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      },
      "offset": {
        "type": "string",
        "description": "开始行数"
      },
      "limit": {
        "type": "string",
        "description": "最大行数"
      }
    },
    "required": []
  }
}