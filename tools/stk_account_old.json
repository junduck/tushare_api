{
  "name": "tushare_stk_account_old",
  "description": "股票开户数据（旧）。获取股票账户开户数据旧版格式数据，数据从2008年1月开始，到2015年5月29，新数据请通过股票开户数据获取。积分：600积分可调取，具体请参阅积分获取办法。返回字段包括：date(Y), new_sh(Y), new_sz(Y), active_sh(Y), active_sz(Y), total_sh(Y), total_sz(Y), trade_sh(Y), trade_sz(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}