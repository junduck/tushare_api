{
  "name": "tushare_report_rc",
  "description": "券商盈利预测数据。获取券商（卖方）每天研报的盈利预测数据，数据从2010年开始，每晚19~22点更新当日数据限量：单次最大3000条，可分页和循环提取所有数据权限：本接口120积分可以试用，每天10次请求，正式权限需8000积分，每天可请求100000次，10000积分以上无总量限制。。返回字段包括：ts_code(Y), name(Y), report_date(Y), report_title(Y), report_type(Y), classify(Y), org_name(Y), author_name(Y), quarter(Y), op_rt(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "report_date": {
        "type": "string",
        "description": "报告日期",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "报告开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "报告结束日期",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}