{
  "name": "tushare_rt_etf_k",
  "description": "ETF实时日线。获取ETF实时日k线行情，支持按ETF代码或代码通配符一次性提取全部ETF实时日k线行情限量：单次最大可提取2000条数据积分：本接口是单独开权限的数据，单独申请权限请参考权限列表。返回字段包括：ts_code(Y), name(Y), pre_close(Y), high(Y), open(Y), low(Y), close(Y), vol(Y), amount(Y), num(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "支持通配符方式，e.g. 5*.SH、15*.SZ、159101.SZ"
      },
      "topic": {
        "type": "string",
        "description": "分类参数，取上海ETF时，需要输入'HQ_FND_TICK'，参考下面例子"
      }
    },
    "required": [
      "ts_code",
      "topic"
    ]
  }
}