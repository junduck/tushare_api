{
  "name": "tushare_fut_wsr",
  "description": "仓单日报。获取仓单日报数据，了解各仓库/厂库的仓单变化限量：单次最大1000，总量不限制积分：用户需要至少2000积分才可以调取，具体请参阅积分获取办法。返回字段包括：trade_date(Y), symbol(Y), fut_name(Y), warehouse(Y), wh_id(N), pre_vol(Y), vol(Y), vol_chg(Y), area(N), year(N)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "trade_date": {
        "type": "string",
        "description": "交易日期",
        "pattern": "^\\d{8}$"
      },
      "symbol": {
        "type": "string",
        "description": "产品代码"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期(YYYYMMDD格式，下同)",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      },
      "exchange": {
        "type": "string",
        "description": "交易所代码"
      }
    },
    "required": []
  }
}