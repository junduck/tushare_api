{
  "name": "tushare_us_income",
  "description": "美股利润表。获取美股上市公司财务利润表数据（目前只覆盖主要美股和中概股）权限：需单独开权限或有15000积分，具体权限信息请参考权限列表提示：当前接口按单只股票获取其历史数据，单次请求最大返回10000行数据，可循环提取。返回字段包括：ts_code(Y), end_date(Y), ind_type(Y), name(Y), ind_name(Y), ind_value(Y), report_type(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "period": {
        "type": "string",
        "description": "报告期（格式：YYYYMMDD，每个季度最后一天的日期，如20241231)"
      },
      "ind_name": {
        "type": "string",
        "description": "指标名(如：新增借款）"
      },
      "report_type": {
        "type": "string",
        "description": "报告期类型(Q1一季报Q2半年报Q3三季报Q4年报)"
      },
      "start_date": {
        "type": "string",
        "description": "报告期开始时间（格式：YYYYMMDD）",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "报告结束始时间（格式：YYYYMMDD）",
        "pattern": "^\\d{8}$"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}