{
  "name": "tushare_bo_weekly",
  "description": "电影周度票房。获取周度票房数据数据更新：本周更新上一周数据数据历史： 数据从2008年第一周开始，超过10年历史数据。数据权限：用户需要至少500积分才可以调取，具体请参阅积分获取办法。返回字段包括：date(Y), name(Y), avg_price(Y), week_amount(Y), total(Y), list_day(Y), p_pc(Y), wom_index(Y), up_ratio(Y), rank(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "日期（每周一日期，格式YYYYMMDD）",
        "pattern": "^\\d{8}$"
      }
    },
    "required": [
      "date"
    ]
  }
}