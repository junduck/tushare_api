{
  "name": "tushare_daily_basic",
  "description": "每日指标。获取全部股票每日重要的基本面指标，可用于选股分析、报表展示等。单次请求最大返回6000条数据，可按日线循环提取全部历史。积分：至少2000积分才可以调取，5000积分无总量限制，具体请参阅积分获取办法。返回字段包括：ts_code(TS股票代码), trade_date(交易日期), close(当日收盘价), turnover_rate(换手率（%）), turnover_rate_f(换手率（自由流通股）), volume_ratio(量比), pe(市盈率（总市值/净利润， 亏损的PE为空）), pe_ttm(市盈率（TTM，亏损的PE为空）), pb(市净率（总市值/净资产）), ps(市销率)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码（二选一）"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期 （二选一）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期(YYYYMMDD)",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期(YYYYMMDD)",
        "pattern": "^\\d{8}$"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}