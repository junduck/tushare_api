{
  "name": "tushare_stk_auction_o",
  "description": "股票开盘集合竞价数据。股票开盘9:30集合竞价数据，每天盘后更新限量：单次请求最大返回10000行数据，可根据日期循环权限：开通了股票分钟权限后可获得本接口权限，具体参考权限说明。返回字段包括：ts_code(Y), trade_date(Y), close(Y), open(Y), high(Y), low(Y), vol(Y), amount(Y), vwap(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期(YYYYMMDD)",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期(YYYYMMDD)",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期(YYYYMMDD)",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}