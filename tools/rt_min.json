{
  "name": "tushare_rt_min",
  "description": "实时分钟。获取全A股票实时分钟数据，包括1~60min限量：单次最大1000行数据，可以通过股票代码提取数据，支持逗号分隔的多个代码同时提取权限：正式权限请参阅 权限说明。返回字段包括：ts_code(Y), time(Y), open(Y), close(Y), high(Y), low(Y), vol(Y), amount(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "freq": {
        "type": "string",
        "description": "1MIN,5MIN,15MIN,30MIN,60MIN （大写）"
      },
      "ts_code": {
        "type": "string",
        "description": "支持单个和多个：600000.SH 或者 600000.SH,000001.SZ"
      }
    },
    "required": [
      "freq",
      "ts_code"
    ]
  }
}