{
  "name": "tushare_irm_qa_sh",
  "description": "上证e互动问答。获取上交所e互动董秘问答文本数据。上证e互动是由上海证券交易所建立、上海证券市场所有参与主体无偿使用的沟通平台,旨在引导和促进上市公司、投资者等各市场参与主体之间的信息沟通,构建集中、便捷的互动渠道。本接口数据记录了以上沟通问答的文本数据。限量：单次请求最大返回3000行数据，可根据股票代码，日期等参数循环提取全部数据权限：用户后120积分可以试用，正式权限为10000积分，或申请单独开权限，请参考权限说明。返回字段包括：ts_code(Y), name(Y), trade_date(Y), q(Y), a(Y), pub_time(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期（格式YYYYMMDD，下同）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      },
      "pub_date": {
        "type": "string",
        "description": "发布开始日期(格式：2025-06-03 16:43:03)",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}