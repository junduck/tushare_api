{
  "name": "tushare_fx_obasic",
  "description": "外汇基础信息（海外）。获取海外外汇基础信息，目前只有FXCM交易商的数据数量：单次可提取全部数据积分：用户需要至少2000积分才可以调取，具体请参阅积分获取办法。返回字段包括：ts_code(Y), name(Y), classify(Y), exchange(Y), min_unit(Y), max_unit(Y), pip(Y), pip_cost(Y), traget_spread(Y), min_stop_distance(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "exchange": {
        "type": "string",
        "description": "交易商"
      },
      "classify": {
        "type": "string",
        "description": "分类"
      },
      "ts_code": {
        "type": "string",
        "description": "TS代码"
      }
    },
    "required": []
  }
}