{
  "name": "tushare_cb_basic",
  "description": "可转债基础信息。获取可转债基本信息限量：单次最大2000，总量不限制权限：用户需要至少2000积分才可以调取，但有流量控制，5000积分以上频次相对较高，积分越多权限越大，具体请参阅积分获取办法。返回字段包括：ts_code(Y), bond_full_name(Y), bond_short_name(Y), cb_code(Y), stk_code(Y), stk_short_name(Y), maturity(Y), par(Y), issue_price(Y), issue_size(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "转债代码"
      },
      "list_date": {
        "type": "string",
        "description": "上市日期",
        "pattern": "^\\d{8}$"
      },
      "exchange": {
        "type": "string",
        "description": "上市地点"
      }
    },
    "required": []
  }
}