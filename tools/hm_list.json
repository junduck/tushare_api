{
  "name": "tushare_hm_list",
  "description": "市场游资最全名录。获取游资分类名录信息限量：单次最大1000条数据，目前总量未超过500积分：5000积分可以调取，积分获取办法请参阅积分获取办法。返回字段包括：name(Y), desc(Y), orgs(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "游资名称"
      }
    },
    "required": []
  }
}