{
  "name": "tushare_rt_k",
  "description": "实时日线。获取实时日k线行情，支持按股票代码及股票代码通配符一次性提取全部股票实时日k线行情限量：单次最大可提取6000条数据，等同于一次提取全市场积分：本接口是单独开权限的数据，单独申请权限请参考权限列表。返回字段包括：ts_code(Y), name(Y), pre_close(Y), high(Y), open(Y), low(Y), close(Y), vol(Y), amount(Y), num(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "支持通配符方式，e.g. 6*.SH、301*.SZ、600000.SH"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}