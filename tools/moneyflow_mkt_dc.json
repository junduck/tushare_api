{
  "name": "tushare_moneyflow_mkt_dc",
  "description": "大盘资金流向（DC）。获取东方财富大盘资金流向数据，每日盘后更新限量：单次最大3000条，可根据日期或日期区间循环获取积分：120积分可试用，5000积分可正式调取，具体请参阅积分获取办法。返回字段包括：trade_date(Y), close_sh(Y), pct_change_sh(Y), close_sz(Y), pct_change_sz(Y), net_amount(Y), net_amount_rate(Y), buy_elg_amount(Y), buy_elg_amount_rate(Y), buy_lg_amount(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "trade_date": {
        "type": "string",
        "description": "交易日期(YYYYMMDD格式，下同）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}