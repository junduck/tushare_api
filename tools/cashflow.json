{
  "name": "tushare_cashflow",
  "description": "现金流量表。获取上市公司现金流量表积分：用户需要至少2000积分才可以调取，具体请参阅积分获取办法提示：当前接口只能按单只股票获取其历史数据，如果需要获取某一季度全部上市公司数据，请使用cashflow_vip接口（参数一致），需积攒5000积分。。返回字段包括：ts_code(Y), ann_date(Y), f_ann_date(Y), end_date(Y), comp_type(Y), report_type(Y), end_type(Y), net_profit(Y), finan_exp(Y), c_fr_sale_sg(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码"
      },
      "ann_date": {
        "type": "string",
        "description": "公告日期（YYYYMMDD格式，下同）",
        "pattern": "^\\d{8}$"
      },
      "f_ann_date": {
        "type": "string",
        "description": "实际公告日期",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "公告开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "公告结束日期",
        "pattern": "^\\d{8}$"
      },
      "period": {
        "type": "string",
        "description": "报告期(每个季度最后一天的日期，比如20171231表示年报，20170630半年报，20170930三季报)"
      },
      "report_type": {
        "type": "string",
        "description": "报告类型：见下方详细说明"
      },
      "comp_type": {
        "type": "string",
        "description": "公司类型：1一般工商业 2银行 3保险 4证券"
      },
      "is_calc": {
        "type": "number",
        "description": "是否计算报表"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}