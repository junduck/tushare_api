{
  "name": "tushare_rt_hk_k",
  "description": "港股实时日线。获取港股实时日k线行情，支持按股票代码及股票代码通配符一次性提取全部股票实时日k线行情限量：单次最大可提取5000条数据积分：本接口是单独开权限的数据，单独申请权限请参考权限列表。返回字段包括：ts_code(Y), pre_close(Y), close(Y), high(Y), open(Y), low(Y), vol(Y), amount(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "支持通配符方式，e.g. 00001.HK、02*.HK"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}