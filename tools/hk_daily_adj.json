{
  "name": "tushare_hk_daily_adj",
  "description": "港股复权行情。获取港股复权行情，提供股票股本、市值和成交及换手多个数据指标限量：单次最大可以提取6000条数据，可循环获取全部，支持分页提取要求：120积分可以试用查看数据，开通正式权限请参考权限说明文档。返回字段包括：ts_code(Y), trade_date(Y), close(Y), open(Y), high(Y), low(Y), pre_close(Y), change(Y), pct_change(Y), vol(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "股票代码（e.g. 00001.HK）"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期（YYYYMMDD）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期（YYYYMMDD）",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期（YYYYMMDD）",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}