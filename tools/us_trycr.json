{
  "name": "tushare_us_trycr",
  "description": "国债实际收益率曲线利率。国债实际收益率曲线利率限量：单次最大可获取2000行数据，可循环获取权限：用户积累120积分可以使用，积分越高频次越高。具体请参阅积分获取办法。返回字段包括：date(Y), y5(Y), y7(Y), y10(Y), y20(Y), y30(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "日期 （YYYYMMDD格式，下同）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      },
      "fields": {
        "type": "string",
        "description": "指定输出字段"
      }
    },
    "required": []
  }
}