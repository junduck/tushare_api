{
  "name": "tushare_block_trade",
  "description": "大宗交易。大宗交易限量：单次最大1000条，总量不限制积分：300积分可调取，每分钟内限制次数，超过5000积分频次相对较高，具体请参阅积分获取办法。返回字段包括：ts_code(Y), trade_date(Y), price(Y), vol(Y), amount(Y), buyer(Y), seller(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "TS代码（股票代码和日期至少输入一个参数）"
      },
      "trade_date": {
        "type": "string",
        "description": "交易日期（格式：YYYYMMDD，下同）",
        "pattern": "^\\d{8}$"
      },
      "start_date": {
        "type": "string",
        "description": "开始日期",
        "pattern": "^\\d{8}$"
      },
      "end_date": {
        "type": "string",
        "description": "结束日期",
        "pattern": "^\\d{8}$"
      }
    },
    "required": []
  }
}