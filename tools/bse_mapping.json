{
  "name": "tushare_bse_mapping",
  "description": "北交所新旧代码对照。获取北交所股票代码变更后新旧代码映射表数据限量：单次最大1000条（本接口总数据量300以内）积分：120积分即可调取。返回字段包括：name(Y), o_code(Y), n_code(Y), list_date(Y)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "o_code": {
        "type": "string",
        "description": "旧代码"
      },
      "n_code": {
        "type": "string",
        "description": "新代码"
      }
    },
    "required": []
  }
}