{
  "name": "tushare_cb_rate",
  "description": "可转债票面利率。获取可转债票面利率限量：单次最大2000，总量不限制权限：用户需要至少5000积分才可以调取，具体请参阅积分获取办法。返回字段包括：ts_code(Y), rate_freq(N), rate_start_date(N), rate_end_date(N), coupon_rate(N)。",
  "inputSchema": {
    "type": "object",
    "properties": {
      "ts_code": {
        "type": "string",
        "description": "转债代码，支持多值输入"
      }
    },
    "required": [
      "ts_code"
    ]
  }
}